# Summary

<!-- 1–3 bullets on what changed and why. -->

## Related issue

<!-- Closes #N (every PR has an issue per Hard Rule 26). -->

## Test plan

- [ ] Bench-tested on hardware (specify what was tested):
- [ ] `idf.py build` clean (no new warnings)
- [ ] Behaviour matches the wiki spec where applicable

## UI spec sync (skip if PR doesn't touch UI behaviour)

> The on-device UI spec lives in the firmware repo's [GitHub Wiki](https://github.com/Temple-of-Epiphany/anchor-drag-pro/wiki). See [Spec-Sync](https://github.com/Temple-of-Epiphany/anchor-drag-pro/wiki/Spec-Sync) for the rule.

- [ ] Wiki spec updated to match this PR — link:
  `<wiki page URL or commit SHA>`
- [ ] OR: internal-only change (refactor / perf / build / test). Justification:
  `<one sentence>`

## Checklist

- [ ] No secrets committed
- [ ] 3rd-party actions pinned to SHA (if `.github/workflows/*` changed)
- [ ] CHANGELOG `[Unreleased]` updated (if user-visible)
- [ ] Cross-repo PRs linked (if multi-repo change)
