# tokens/ — design tokens (palette + semantic)

Cross-platform color (later: typography, spacing, motion) tokens. Single source generates platform-native files (`anchor_colors.h` for LVGL, `AnchorColors.swift` for iOS, `colors.css` for web).

Empty until [Workstream 0 prereq #45 (design token system)](https://github.com/Temple-of-Epiphany/anchor-drag-pro/issues/45) lands.

Spec: [`docs/design-tokens.md`](https://github.com/Temple-of-Epiphany/anchor-drag-pro-meta/blob/main/docs/design-tokens.md) in the meta-repo.

Will eventually contain `palette.json` + `semantic.day.json` (+ `semantic.night.json` later).
