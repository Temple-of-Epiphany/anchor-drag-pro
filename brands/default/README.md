# brands/default — Anchor Drag Pro

The default brand. Empty until [Workstream 0 prereq #46 (brand abstraction)](https://github.com/Temple-of-Epiphany/anchor-drag-pro/issues/46) populates it.

Will eventually contain `brand.json` + `assets/` (logo, icons).
